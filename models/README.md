# Models

This folder is reserved for local VLMs data.

## Notes

- Keep large weights and caches out of version control.
- Match the model paths here to the values used in the scripts under `control/`.
- The folder is empty by default and can be populated as needed for local runs.